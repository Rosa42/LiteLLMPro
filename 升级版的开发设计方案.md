# 基于 LiteLLM 的共享 Coding Plan 智能路由系统设计

## 1. 项目目标

构建一个本地运行、OpenAI API 兼容的多套餐智能中转站。

当前资源：

| 套餐账号          | 提供商      | 可用模型            | 额度关系        |
| ------------- | -------- | --------------- | ----------- |
| OpenCode Go A | OpenCode | Kimi K3、GLM-5.2 | 账号内所有模型共享额度 |
| OpenCode Go B | OpenCode | Kimi K3、GLM-5.2 | 账号内所有模型共享额度 |
| Coding Plan C | 火山引擎     | Kimi K3、GLM-5.2 | 账号内所有模型共享额度 |

客户端只看到两个逻辑模型：

```text
kimi-k3
glm-5.2
```

中转站负责：

1. 将同名模型按模型组聚合。
2. 将同一套餐下的所有模型绑定到同一个共享额度组。
3. 某套餐通过任意模型耗尽额度时，立即停用该套餐下的全部模型。
4. 自动在其他仍有额度、且支持相同模型的套餐间切换。
5. 额度恢复后，通过探测重新加入候选池。
6. 保持 LiteLLM 上游可持续升级，不长期维护重度 Fork。

---

# 2. 总体架构

```text
Coding Agent / OpenCode / IDE / CLI
                │
                │ OpenAI-compatible API
                ▼
        LiteLLM Proxy
                │
    ┌───────────┴────────────┐
    │ Shared Quota Extension │
    │                        │
    │ 1. 自定义路由策略       │
    │ 2. 失败分类回调         │
    │ 3. 账号级额度熔断       │
    │ 4. 恢复探测             │
    └───────────┬────────────┘
                │
              Redis
                │
      ┌─────────┼─────────┐
      ▼         ▼         ▼
OpenCode A  OpenCode B  火山 Plan C
```

组件职责：

| 组件               | 职责                                         |
| ---------------- | ------------------------------------------ |
| LiteLLM Proxy    | API 兼容、协议转换、流式传输、Tool Calling、调用上游、基础重试和日志 |
| Quota Router     | 模型候选过滤、套餐账号选择、共享额度熔断                       |
| Error Classifier | 将不同供应商错误归类为套餐耗尽、短期限流、鉴权错误、端点故障等            |
| Redis            | 保存套餐、deployment、供应商状态及恢复时间                 |
| PostgreSQL       | LiteLLM 管理数据、虚拟 Key、日志和 UI；纯个人本地模式可先不启用    |
| Probe Worker     | 检查冷却账号是否恢复                                 |
| Prometheus，可选    | 指标采集与告警                                    |

LiteLLM 官方支持 Router 自定义路由策略，并允许通过 `CustomLogger` 的成功、失败和请求前 Hook 扩展调用流程。

---

# 3. 核心设计原则

## 3.1 不直接修改 LiteLLM 核心代码

优先级依次如下：

1. 官方自定义 Routing Strategy。
2. 官方 Custom Callback / Proxy Hook。
3. 自定义启动 Bootstrap。
4. 最小化 Git Patch。
5. 最后才考虑长期 Fork。

禁止直接在 LiteLLM 源码中散落修改。

如果当前 LiteLLM Proxy 版本无法通过公开配置注册自定义路由策略，只允许维护一个最小补丁：

```text
patches/0001-register-shared-quota-router.patch
```

该补丁只能完成“将自定义策略对象注册到 Router”这一件事，禁止包含额度业务逻辑。

所有业务逻辑必须位于独立 Python 包：

```text
plugins/shared_quota_router/
```

---

## 3.2 双重聚类模型

系统同时维护两个相互独立的维度。

### 模型组

用于回答：

> 哪些 deployment 可以处理 `kimi-k3` 请求？

```text
model_group:kimi-k3
├── opencode-a/kimi-k3
├── opencode-b/kimi-k3
└── volc-c/kimi-k3
```

### 共享额度组

用于回答：

> 哪些 deployment 共享同一个套餐额度？

```text
quota_group:opencode-a
├── opencode-a/kimi-k3
└── opencode-a/glm-5.2
```

不能将模型组状态等同于额度组状态。

---

# 4. 资源数据模型

## 4.1 Provider

```python
class Provider:
    provider_id: str
    name: str
    status: str
    base_url: str
    classifier_type: str
```

状态：

```text
AVAILABLE
DEGRADED
COOLDOWN
DISABLED
```

---

## 4.2 QuotaGroup

一个 QuotaGroup 对应一个 Coding Plan 套餐账号。

```python
class QuotaGroup:
    quota_group_id: str
    provider_id: str
    account_id: str
    display_name: str

    status: str
    priority: int

    reset_at: datetime | None
    cooldown_until: datetime | None

    failure_reason: str | None
    last_failure_at: datetime | None
    last_success_at: datetime | None

    consecutive_failures: int
    revision: int
```

状态机：

```text
AVAILABLE
    │
    ├── 短期异常 ──> DEGRADED
    │
    ├── 套餐耗尽 ──> EXHAUSTED
    │
    ├── Key 失效 ──> DISABLED
    │
    └── 人工暂停 ──> DISABLED

EXHAUSTED
    │
    └── 到达恢复时间 ──> PROBING

PROBING
    ├── 探针成功 ──> AVAILABLE
    └── 探针失败 ──> EXHAUSTED
```

---

## 4.3 QuotaWindow

一个套餐可能同时受多个窗口控制：

```python
class QuotaWindow:
    quota_group_id: str
    window_type: str
    status: str
    remaining: float | None
    reset_at: datetime | None
    observed_at: datetime
```

窗口类型：

```text
FIVE_HOUR
DAILY
WEEKLY
MONTHLY
UNKNOWN
```

只要任意硬额度窗口耗尽，整个 QuotaGroup 即不可用。

---

## 4.4 Deployment

每一个“套餐账号 × 模型”是一个 deployment。

```python
class Deployment:
    deployment_id: str
    model_group: str
    upstream_model: str

    provider_id: str
    quota_group_id: str

    priority: int
    weight: int
    enabled: bool
```

示例：

```text
opencode-a-kimi
opencode-a-glm
opencode-b-kimi
opencode-b-glm
volc-c-kimi
volc-c-glm
```

---

# 5. LiteLLM 模型配置

```yaml
model_list:
  - model_name: kimi-k3
    model_info:
      deployment_id: opencode-a-kimi
      provider_id: opencode-go
      account_id: opencode-a
      quota_group_id: opencode-a
      priority: 10
    litellm_params:
      model: openai/kimi-k3
      api_base: os.environ/OPENCODE_GO_BASE_URL
      api_key: os.environ/OPENCODE_GO_KEY_A
      timeout: 300

  - model_name: glm-5.2
    model_info:
      deployment_id: opencode-a-glm
      provider_id: opencode-go
      account_id: opencode-a
      quota_group_id: opencode-a
      priority: 10
    litellm_params:
      model: openai/glm-5.2
      api_base: os.environ/OPENCODE_GO_BASE_URL
      api_key: os.environ/OPENCODE_GO_KEY_A
      timeout: 300

  - model_name: kimi-k3
    model_info:
      deployment_id: opencode-b-kimi
      provider_id: opencode-go
      account_id: opencode-b
      quota_group_id: opencode-b
      priority: 20
    litellm_params:
      model: openai/kimi-k3
      api_base: os.environ/OPENCODE_GO_BASE_URL
      api_key: os.environ/OPENCODE_GO_KEY_B
      timeout: 300

  - model_name: glm-5.2
    model_info:
      deployment_id: opencode-b-glm
      provider_id: opencode-go
      account_id: opencode-b
      quota_group_id: opencode-b
      priority: 20
    litellm_params:
      model: openai/glm-5.2
      api_base: os.environ/OPENCODE_GO_BASE_URL
      api_key: os.environ/OPENCODE_GO_KEY_B
      timeout: 300

  - model_name: kimi-k3
    model_info:
      deployment_id: volc-c-kimi
      provider_id: volcengine
      account_id: volc-c
      quota_group_id: volc-c
      priority: 30
    litellm_params:
      model: openai/kimi-k3
      api_base: os.environ/VOLC_CODING_BASE_URL
      api_key: os.environ/VOLC_CODING_KEY_C
      timeout: 300

  - model_name: glm-5.2
    model_info:
      deployment_id: volc-c-glm
      provider_id: volcengine
      account_id: volc-c
      quota_group_id: volc-c
      priority: 30
    litellm_params:
      model: openai/glm-5.2
      api_base: os.environ/VOLC_CODING_BASE_URL
      api_key: os.environ/VOLC_CODING_KEY_C
      timeout: 300

router_settings:
  routing_strategy: simple-shuffle
  num_retries: 2
  allowed_fails: 1
  cooldown_time: 30
  retry_after: 1

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY

litellm_settings:
  callbacks:
    - shared_quota_router.callback_instance
```

注意：

* 实际 `api_base`、模型 ID 和参数必须通过套餐控制台或 `/models` 接口确认。
* 不允许把 API Key 写入 YAML。
* `priority` 是自定义业务字段。
* LiteLLM 原生 cooldown 只作为 deployment 短期保护，不能代表套餐 5 小时额度状态。
* 不配置默认的 Kimi → GLM 跨模型 fallback。只有用户明确允许时才能启用跨模型降级。

---

# 6. 路由选择算法

## 6.1 候选生成

收到：

```json
{
  "model": "kimi-k3"
}
```

处理步骤：

```text
1. 获取 model_group=kimi-k3 的所有 deployment
2. 排除 disabled deployment
3. 排除 provider 不可用的 deployment
4. 排除 quota_group 不可用的 deployment
5. 排除 deployment 自身处于 cooldown 的 deployment
6. 按优先级和调度策略排序
7. 选择候选
```

伪代码：

```python
async def get_candidates(model_name: str) -> list[Deployment]:
    deployments = registry.get_by_model_group(model_name)

    candidates = []

    for deployment in deployments:
        if not deployment.enabled:
            continue

        provider_state = await state_store.get_provider(
            deployment.provider_id
        )
        if provider_state.status not in {"AVAILABLE", "DEGRADED"}:
            continue

        quota_state = await state_store.get_quota_group(
            deployment.quota_group_id
        )
        if quota_state.status != "AVAILABLE":
            continue

        deployment_state = await state_store.get_deployment(
            deployment.deployment_id
        )
        if deployment_state.is_in_cooldown:
            continue

        candidates.append(deployment)

    return candidates
```

---

## 6.2 默认调度策略

采用：

```text
Fill First + Session Affinity + Failover
```

排序规则：

1. 已经绑定当前会话的可用 deployment。
2. `priority` 数字较小的套餐。
3. 当前并发更低的套餐。
4. 最近成功过的套餐。
5. 稳定排序的 `deployment_id`。

目的：

* 避免多个套餐被同时打满。
* 错开各套餐的额度恢复窗口。
* 尽可能利用上游 Prompt Cache。
* 同一会话尽量保持上游一致。
* 出错时仍可立即切换。

---

## 6.3 会话粘性

优先从以下字段生成 session key：

```text
metadata.session_id
user
conversation_id
自定义 X-LLM-Session-ID 请求头
```

如果均不存在：

```text
SHA256(model + 前两条消息摘要 + client_api_key_id)
```

Redis：

```text
affinity:{session_hash} -> deployment_id
TTL = 2 小时
```

会话粘性不是硬约束。

如果对应 QuotaGroup 已耗尽，立即忽略并重新选择。

---

## 6.4 并发租约

为避免多个并发请求同时选中即将耗尽的账号，选路时创建短期租约：

```text
lease:{quota_group_id}:{request_id}
TTL = 请求超时时间 + 30 秒
```

Redis Lua 脚本原子完成：

1. 检查 QuotaGroup 状态。
2. 检查当前并发。
3. 增加 inflight。
4. 建立请求租约。

请求结束后释放租约。

进程异常退出时通过 TTL 自动释放。

---

# 7. 请求级切换流程

```text
请求 kimi-k3
    │
    ▼
选中 OpenCode A / kimi-k3
    │
    ├── 成功
    │     ├── 返回客户端
    │     ├── 记录 success
    │     └── 维持 session affinity
    │
    └── 失败
          │
          ▼
       错误分类
          │
          ├── 套餐额度耗尽
          │      ├── quota_group:opencode-a = EXHAUSTED
          │      ├── 清除该账号所有 affinity
          │      └── 选择 B 或 C 重试
          │
          ├── 瞬时限流
          │      ├── 只冷却当前 deployment
          │      └── 选择其他 deployment
          │
          ├── Provider 故障
          │      ├── provider 进入短期 cooldown
          │      └── 切换其他 Provider
          │
          └── 客户端错误
                 └── 不重试，直接返回
```

每个请求最多：

```text
最多尝试 3 个不同 quota_group
同一个 quota_group 最多尝试一次
总尝试次数不超过可用套餐数量
```

禁止：

* 对已经明确耗尽的套餐重复请求。
* 在同一请求内无限重试。
* 因请求参数错误切换账号。
* 因内容审核错误切换账号。
* 同时对同一账号的 Kimi 和 GLM 做无效探测。

---

# 8. 错误分类系统

## 8.1 标准错误类型

```python
class FailureKind(Enum):
    SHARED_QUOTA_EXHAUSTED = "shared_quota_exhausted"
    SHORT_RATE_LIMIT = "short_rate_limit"
    AUTH_INVALID = "auth_invalid"
    ACCOUNT_DISABLED = "account_disabled"
    PROVIDER_OUTAGE = "provider_outage"
    DEPLOYMENT_ERROR = "deployment_error"
    CONTEXT_LIMIT = "context_limit"
    CONTENT_POLICY = "content_policy"
    BAD_REQUEST = "bad_request"
    NETWORK_ERROR = "network_error"
    UNKNOWN = "unknown"
```

---

## 8.2 熔断范围

| 错误类型                   | 处理范围               |   是否切换 |
| ---------------------- | ------------------ | -----: |
| SHARED_QUOTA_EXHAUSTED | 整个 QuotaGroup      |      是 |
| SHORT_RATE_LIMIT       | 当前 deployment，短期   |      是 |
| AUTH_INVALID           | 整个 QuotaGroup，人工处理 |      是 |
| ACCOUNT_DISABLED       | 整个 QuotaGroup      |      是 |
| PROVIDER_OUTAGE        | 整个 Provider，短期     |      是 |
| DEPLOYMENT_ERROR       | 当前 deployment      |      是 |
| CONTEXT_LIMIT          | 当前请求               | 通常不切账号 |
| CONTENT_POLICY         | 当前请求               |      否 |
| BAD_REQUEST            | 当前请求               |      否 |
| NETWORK_ERROR          | 当前 deployment，短期   |      是 |
| UNKNOWN                | 当前 deployment 短期   | 最多切换一次 |

---

## 8.3 Provider 适配器

```text
classifiers/
├── base.py
├── opencode_go.py
├── volcengine.py
└── generic_openai.py
```

统一输出：

```python
@dataclass
class FailureClassification:
    kind: FailureKind
    retryable: bool
    scope: str
    retry_after_seconds: int | None
    reset_at: datetime | None
    confidence: float
    normalized_message: str
```

判断依据按优先级：

1. HTTP 状态码。
2. 响应 JSON 中的标准错误码。
3. 响应 Header：

   * `Retry-After`
   * reset headers
4. 明确的错误文本。
5. Provider 专属规则。
6. 通用兜底规则。

只有高置信度的套餐耗尽错误，才能把整个 QuotaGroup 标记为 `EXHAUSTED`。

低置信度错误只能产生 deployment 级短期 cooldown。

---

# 9. 流式请求的特殊处理

流式请求不能在已向客户端发送内容后透明切换上游。

规则：

```text
上游尚未发送任何响应字节
→ 可以切换另一个套餐并重试

上游已经发送至少一个 SSE chunk
→ 禁止切换
→ 将错误返回客户端
→ 更新套餐状态，供下一次请求使用
```

必须跟踪：

```python
stream_state.first_byte_sent
```

否则会出现：

* 两个模型输出拼接；
* 重复 Tool Call；
* JSON 流损坏；
* 客户端状态错乱。

---

# 10. 额度耗尽处理

## 10.1 有明确 reset_at

```python
await quota_store.mark_exhausted(
    quota_group_id="opencode-a",
    reset_at=provider_reset_at,
    reason="five_hour_quota_exhausted",
)
```

Redis TTL 设置为：

```text
reset_at - now
```

---

## 10.2 只有 Retry-After

转换为：

```text
reset_at = now + Retry-After
```

---

## 10.3 没有恢复时间

不得直接假定“从额度耗尽时起五小时后恢复”。

采用探测计划：

```text
首次探测：5 分钟后
第二次：15 分钟后
第三次：30 分钟后
之后：每 60 分钟
最大间隔：2 小时
```

如果已知该套餐最多五小时恢复，也只将五小时作为探测上限参考，不作为绝对事实。

---

# 11. 恢复探针

恢复 Worker 每分钟查询：

```text
status=EXHAUSTED
且 next_probe_at <= now
```

处理：

```text
EXHAUSTED
    ↓
原子切换为 PROBING
    ↓
发送低成本测试请求
    │
    ├── 成功 → AVAILABLE
    └── 失败 → EXHAUSTED，并安排下一探测
```

探针要求：

* `max_tokens` 设置为供应商允许的最小值。
* 使用固定短 Prompt。
* 超时 15 秒。
* 任何时刻一个 QuotaGroup 只能存在一个探针。
* 探针不走普通用户路由。
* 探针不触发跨套餐 fallback。

---

# 12. Redis 结构

```text
sq:provider:{provider_id}
sq:quota:{quota_group_id}
sq:window:{quota_group_id}:{window_type}
sq:deployment:{deployment_id}
sq:affinity:{session_hash}
sq:lease:{quota_group_id}:{request_id}
sq:probe-lock:{quota_group_id}
sq:audit:{date}
```

示例：

```json
{
  "quota_group_id": "opencode-a",
  "status": "EXHAUSTED",
  "reason": "five_hour_quota",
  "reset_at": "2026-07-18T07:30:00Z",
  "next_probe_at": "2026-07-18T07:30:05Z",
  "last_model": "kimi-k3",
  "last_http_status": 429,
  "revision": 17
}
```

要求：

* 所有状态变更使用 Lua 或事务。
* 使用 revision 实现乐观锁。
* 状态 Key 设置合理 TTL。
* 审计记录不得包含 API Key、完整 Prompt 或响应内容。
* Redis 不可用时采取 fail-closed：不自动启用未知状态的套餐。

---

# 13. 项目目录

```text
local-llm-router/
├── README.md
├── AGENTS.md
├── .env.example
├── .gitignore
├── pyproject.toml
├── uv.lock
│
├── upstream/
│   └── litellm/                  # 官方仓库，只读 submodule
│
├── plugins/
│   └── shared_quota_router/
│       ├── __init__.py
│       ├── bootstrap.py
│       ├── strategy.py
│       ├── callbacks.py
│       ├── registry.py
│       ├── models.py
│       ├── state_store.py
│       ├── lease.py
│       ├── recovery_worker.py
│       ├── metrics.py
│       └── classifiers/
│           ├── base.py
│           ├── generic_openai.py
│           ├── opencode_go.py
│           └── volcengine.py
│
├── config/
│   ├── litellm.yaml
│   ├── accounts.example.yaml
│   └── logging.yaml
│
├── deploy/
│   ├── docker-compose.yaml
│   ├── Dockerfile.litellm
│   ├── Dockerfile.worker
│   ├── healthcheck.sh
│   └── caddy/
│       └── Caddyfile
│
├── patches/
│   └── README.md
│
├── scripts/
│   ├── bootstrap.sh
│   ├── sync-upstream.sh
│   ├── build.sh
│   ├── smoke-test.sh
│   ├── upgrade.sh
│   ├── rollback.sh
│   └── backup-db.sh
│
├── tests/
│   ├── unit/
│   ├── contract/
│   ├── integration/
│   ├── e2e/
│   ├── upgrade/
│   └── fixtures/
│
└── docs/
    ├── architecture.md
    ├── provider-errors.md
    ├── operations.md
    ├── upgrades.md
    └── rollback.md
```

---

# 14. LiteLLM 上游管理

## 14.1 上游仓库

```bash
git submodule add \
  https://github.com/BerriAI/litellm.git \
  upstream/litellm
```

禁止向 submodule 直接提交业务修改。

本项目自身保持独立仓库。

---

## 14.2 运行方式

生产运行优先使用官方固定版本镜像：

```text
ghcr.io/berriai/litellm:v1.90.5
```

自定义镜像：

```dockerfile
ARG LITELLM_VERSION=v1.90.5
FROM ghcr.io/berriai/litellm:${LITELLM_VERSION}

COPY plugins/shared_quota_router /app/shared_quota_router
COPY config/litellm.yaml /app/config.yaml

ENV PYTHONPATH="/app:${PYTHONPATH}"
```

不得使用：

```text
latest
main
main-latest
dev
nightly
rc
```

官方镜像有签名，升级脚本应在拉取后使用 `cosign verify` 校验。LiteLLM 的发布页提供了固定公钥提交和镜像签名验证方法。

---

# 15. Docker Compose

```yaml
services:
  litellm:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.litellm
      args:
        LITELLM_VERSION: ${LITELLM_VERSION}
    restart: unless-stopped
    env_file:
      - ../.env
    volumes:
      - ../config/litellm.yaml:/app/config.yaml:ro
    command:
      - --config
      - /app/config.yaml
      - --port
      - "4000"
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - internal

  quota-worker:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.worker
    restart: unless-stopped
    env_file:
      - ../.env
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - internal

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command:
      - redis-server
      - --appendonly
      - "yes"
      - --requirepass
      - ${REDIS_PASSWORD}
    volumes:
      - redis-data:/data
    healthcheck:
      test:
        - CMD
        - redis-cli
        - -a
        - ${REDIS_PASSWORD}
        - ping
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - internal

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: litellm
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: litellm
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U litellm
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - internal

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "127.0.0.1:4000:4000"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
    depends_on:
      - litellm
    networks:
      - internal

networks:
  internal:

volumes:
  redis-data:
  postgres-data:
```

本地服务默认只绑定：

```text
127.0.0.1
```

不应直接暴露到公网。

LiteLLM 官方生产部署以 PostgreSQL 作为主要数据库，并建议在多实例和共享路由状态场景使用 Redis。官方也要求生产迁移由单独迁移任务执行，避免多个 Proxy 实例同时修改数据库。

---

# 16. 可观测性

必须产生以下指标：

```text
shared_quota_group_status
shared_quota_failover_total
shared_quota_exhausted_total
shared_quota_probe_total
shared_quota_probe_success_total
shared_quota_classifier_total
shared_quota_classifier_low_confidence_total
shared_quota_route_selection_total
shared_quota_inflight_requests
shared_quota_retry_total
shared_quota_stream_failure_after_first_byte_total
```

关键标签：

```text
provider_id
quota_group_id
deployment_id
model_group
failure_kind
```

禁止把以下内容放进指标标签：

* API Key。
* 用户 Prompt。
* 响应正文。
* 完整错误正文。
* 会话 ID 原文。

日志使用结构化 JSON：

```json
{
  "event": "quota_group_exhausted",
  "request_id": "req_xxx",
  "provider_id": "opencode-go",
  "quota_group_id": "opencode-a",
  "deployment_id": "opencode-a-kimi",
  "model_group": "kimi-k3",
  "reset_at": null,
  "classifier_confidence": 0.98
}
```

LiteLLM 为每次请求生成调用标识，并支持自定义回调和 OpenTelemetry；可将本扩展的 request ID 与 LiteLLM call ID 关联。

---

# 17. 测试计划

## 17.1 单元测试

必须覆盖：

* 模型候选过滤。
* QuotaGroup 状态机。
* Redis 原子状态变更。
* Provider 错误分类。
* reset_at 解析。
* Fill First 排序。
* Session Affinity。
* 探测退避。
* 流式首字节状态。
* 并发租约回收。

---

## 17.2 契约测试

契约测试用于检测 LiteLLM 升级是否破坏扩展点。

每次升级必须验证：

```text
CustomRoutingStrategyBase 仍可导入
async_get_available_deployment 签名兼容
CustomLogger 失败回调仍触发
Proxy Hook 能加载
model_info 自定义字段仍保留
失败回调能够获取 deployment 信息
Router 重试时重新执行 deployment 选择
流式错误回调能够触发
```

任何一项失败，升级必须停止。

---

## 17.3 集成测试场景

### 场景 A：套餐共享额度耗尽

```text
A/kimi 返回套餐耗尽
```

断言：

```text
quota_group A = EXHAUSTED
A/kimi 不可用
A/glm 不可用
kimi 请求自动切换到 B 或 C
glm 请求不能选择 A
```

### 场景 B：短期模型限流

```text
A/kimi 返回普通短期 429
```

断言：

```text
只冷却 A/kimi
A/glm 仍可用
QuotaGroup A 仍为 AVAILABLE
```

### 场景 C：鉴权失效

```text
A/kimi 返回 invalid_api_key
```

断言：

```text
QuotaGroup A = DISABLED
A 下所有模型停用
产生高优先级告警
```

### 场景 D：Provider 故障

```text
OpenCode A 和 B 均超时
```

断言：

```text
OpenCode Provider 短期 DEGRADED/COOLDOWN
请求切换到火山
```

### 场景 E：恢复

```text
A 到达 reset_at
探针成功
```

断言：

```text
QuotaGroup A = AVAILABLE
A/kimi 和 A/glm 同时恢复候选资格
```

### 场景 F：流式中断

```text
A 已输出三个 SSE chunk 后失败
```

断言：

```text
不得拼接 B 的输出
错误返回客户端
A 的状态得到更新
下一请求才能切换
```

---

## 17.4 并发测试

至少模拟：

```text
100 个并发请求
3 个 QuotaGroup
2 个模型组
```

验证：

* 无重复状态覆盖。
* 无负 inflight。
* 一个账号耗尽后，一秒内停止获取新请求。
* Redis 短暂中断不会错误启用已耗尽账号。
* 状态恢复不会发生探针风暴。

---

# 18. 升级流程

## 18.1 版本策略

只接受：

```text
正式稳定版本 vX.Y.Z
```

拒绝：

```text
vX.Y.Z-dev.*
vX.Y.Z-rc.*
nightly
main
latest
```

当前初始固定版本：

```text
v1.90.5
```

稳定版本检查必须读取 GitHub Releases，而不是简单选择最高版本号，因为最高版本可能是 dev 或 rc。

---

## 18.2 AGENT 升级步骤

```text
1. 检查工作区是否干净
2. 读取当前 LITELLM_VERSION
3. 获取官方 tags 和 Releases
4. 选择最新稳定版本
5. 创建 upgrade/litellm-vX.Y.Z 分支
6. 更新 upstream/litellm submodule
7. 下载并验证官方 Docker 镜像签名
8. 阅读当前版本到目标版本间的 release notes
9. 检查重大行为、配置和数据库迁移变化
10. 构建自定义镜像
11. 运行静态检查
12. 运行单元测试
13. 运行 LiteLLM 扩展契约测试
14. 运行模拟 Provider 集成测试
15. 运行端到端 Smoke Test
16. 生成升级报告
17. 只有全部通过才允许更新生产版本文件
```

---

## 18.3 升级前检查

```bash
git status --porcelain
docker compose ps
docker compose exec redis redis-cli ping
docker compose exec postgres pg_isready
```

备份：

```bash
pg_dump \
  -h localhost \
  -U litellm \
  -d litellm \
  -F c \
  -f backups/litellm_$(date +%Y%m%d_%H%M%S).dump
```

导出配置快照：

```text
config/litellm.yaml
.env 中变量名称清单，不导出 Secret 值
当前镜像 digest
当前 Git commit
当前数据库 migration 列表
```

LiteLLM 官方回滚指南明确要求升级或回滚前备份数据库，并要求回滚过程中保持 `LITELLM_SALT_KEY` 不变，否则数据库中加密保存的凭据可能无法解密。

---

## 18.4 数据库迁移

生产迁移必须单独执行：

```text
单一 migration job
    ↓
migration 成功
    ↓
启动新 LiteLLM
```

不得让多个 LiteLLM 实例并行执行 Schema 更新。

建议：

```text
USE_PRISMA_MIGRATE=True
DISABLE_SCHEMA_UPDATE=true  # Proxy 实例
```

迁移任务单独设置：

```text
DISABLE_SCHEMA_UPDATE=false
```

LiteLLM 官方生产建议使用 `prisma migrate deploy`，并在 Helm 场景使用单独 PreSync migration hook。

---

## 18.5 Canary 验证

升级前启动隔离的 Canary：

```text
端口：4001
Redis prefix：sq-canary:
测试 PostgreSQL：独立数据库或数据库快照副本
```

禁止 Canary 与生产共享可写状态。

Canary 验证：

```text
GET /health
GET /v1/models
普通 Chat Completion
流式 Chat Completion
Tool Calling
Kimi 路由切换
GLM 路由切换
套餐级熔断
恢复探针
API Key 鉴权
日志脱敏
```

---

## 18.6 生产切换

```text
1. 停止接收新请求
2. 等待现有请求完成，最多 5 分钟
3. 执行数据库备份
4. 执行迁移任务
5. 启动新版本
6. 健康检查
7. 执行 Smoke Test
8. 切换 Caddy upstream
9. 观察 15 分钟
10. 标记升级成功
```

不得采用覆盖原容器后再测试的方式。

---

# 19. 回滚流程

触发条件：

* Proxy 无法启动。
* 自定义策略未加载。
* 失败回调未触发。
* 路由错误率显著上升。
* 数据库迁移失败。
* Kimi/GLM 流式协议异常。
* Tool Calling 回归。
* 套餐级熔断失效。

应用回滚：

```text
1. 将镜像版本恢复到 previous_version
2. 恢复对应配置
3. 重新部署
4. 执行 Smoke Test
```

数据库处理：

* 仅应用代码异常时，优先保留已向前兼容的数据库。
* Migration 失败或 Schema 不兼容时，使用升级前快照恢复。
* 不允许由 AGENT 猜测性编辑 `_prisma_migrations`。
* 只有明确评估迁移状态后才能修改 migration history。

官方回滚指南建议恢复到前一个稳定版本，并对数据库迁移问题单独处理。

---

# 20. 安全要求

1. API Key 只通过 Secret 或环境变量注入。
2. `.env` 必须加入 `.gitignore`。
3. 日志禁止输出 Authorization Header。
4. 错误响应落盘前必须脱敏。
5. 默认只监听 `127.0.0.1`。
6. LiteLLM Master Key 使用至少 32 字节随机值。
7. Redis 启用密码。
8. PostgreSQL 不暴露宿主机端口。
9. 固定 Docker 镜像 digest。
10. 验证 LiteLLM 镜像签名。
11. AGENT 不得自动提交真实凭据。
12. AGENT 不得自动推送远端仓库，除非显式授权。

---

# 21. 第一阶段实施范围

第一阶段只实现被动额度感知：

```text
上游请求失败
→ 分类共享套餐耗尽
→ QuotaGroup 熔断
→ 同请求切换其他套餐
→ 到期或退避探测恢复
```

暂不实现：

* 抓取供应商非公开额度页面。
* 浏览器 Cookie 自动化。
* 账号自动登录。
* 基于未公开接口的余额查询。
* 预测剩余额度。
* 跨模型自动降级。

先保证切换正确，再加入主动额度采集。

---

# 22. 第二阶段实施范围

供应商存在稳定官方额度接口时增加：

```text
quota_collectors/
├── opencode_go.py
└── volcengine.py
```

能力：

* 定期读取剩余额度。
* 获取 reset_at。
* 低余额预切换。
* 根据恢复时间排序。
* 展示 5 小时、Weekly 和 Monthly 状态。

额度采集失败不得影响请求转发；系统自动退回被动错误识别模式。

---

# 23. 验收标准

系统交付必须满足：

```text
[ ] 客户端只能看到 kimi-k3 和 glm-5.2
[ ] 三个套餐均可以被加载
[ ] 同名模型可跨套餐路由
[ ] 同一套餐下模型共享 QuotaGroup
[ ] A/kimi 耗尽后 A/glm 立即不可用
[ ] 请求能切换到 B 或 C
[ ] 瞬时限流不会错误熔断整个账号
[ ] 流式首字节后不会切换并拼接响应
[ ] QuotaGroup 可自动探测恢复
[ ] Redis 重启后状态可恢复
[ ] LiteLLM 升级契约测试通过
[ ] 版本固定且镜像签名验证通过
[ ] 数据库可备份和回滚
[ ] API Key 不进入日志或 Git
```

---

# 24. 本地 AGENT 执行指令

## 任务

在当前目录创建 `local-llm-router` 项目，基于 LiteLLM 稳定版本实现共享 Coding Plan 路由扩展。

## 强制约束

1. 初始 LiteLLM 版本固定为 `v1.90.5`。
2. 不使用 dev、rc、nightly、main 或 latest。
3. 不直接修改 LiteLLM 业务核心。
4. 优先使用官方 Custom Routing Strategy 和 CustomLogger Hook。
5. 如果 Proxy 无法注册自定义策略，只允许创建一个最小注册补丁。
6. 所有账号共享额度逻辑位于 `plugins/shared_quota_router`。
7. 使用 Redis 保存 QuotaGroup 状态。
8. 同一 QuotaGroup 任一模型额度耗尽时，必须停用该账号的全部模型。
9. 同一请求只有在流式首字节发送前才能切换。
10. 不得写入或提交任何真实 Secret。

## 执行顺序

```text
阶段 1：创建仓库和目录
阶段 2：拉取 LiteLLM 上游仓库
阶段 3：固定 v1.90.5
阶段 4：建立 Docker Compose
阶段 5：实现数据模型和 Redis Store
阶段 6：实现错误分类器
阶段 7：实现自定义路由策略
阶段 8：实现失败回调
阶段 9：实现恢复 Worker
阶段 10：实现模拟 Provider
阶段 11：编写单元和契约测试
阶段 12：编写集成测试
阶段 13：验证流式切换边界
阶段 14：编写升级与回滚脚本
阶段 15：生成操作文档
```

## 每个阶段输出

AGENT 每个阶段必须提交：

```text
变更文件清单
实现说明
执行过的命令
测试结果
未解决风险
下一阶段计划
```

## 禁止行为

```text
不得跳过测试
不得关闭类型检查来通过构建
不得吞掉未知错误
不得把所有 429 都当作套餐耗尽
不得在首字节发送后自动拼接另一个模型
不得未经备份执行数据库升级
不得自动升级到最高版本号而不判断稳定通道
```

---

# 25. 最终工程决策

采用：

```text
官方固定版本 LiteLLM
+ 自定义 Routing Strategy
+ CustomLogger 失败回调
+ Redis QuotaGroup 状态
+ 独立恢复 Worker
+ Provider 错误适配器
+ 最小兼容补丁机制
```

不采用：

```text
长期重度 Fork LiteLLM
纯 deployment 级 cooldown
所有 429 统一冷却五小时
随机平均消耗所有套餐
默认跨模型降级
依赖非公开额度接口作为核心功能
```

这个边界能同时满足：

* 多套餐按模型聚类；
* 共享额度账号级熔断；
* 同请求故障转移；
* LiteLLM 上游持续升级；
* 升级冲突可控；
* 故障和回滚范围清晰。
