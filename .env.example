# Copy to .env and fill values. Never commit .env or real secrets.

# LiteLLM
LITELLM_VERSION=v1.90.5
LITELLM_MASTER_KEY=change-me-to-a-random-value-at-least-32-bytes-long
# Keep stable across upgrades/rollbacks (required for encrypted credentials in DB)
# LITELLM_SALT_KEY=

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=change-me-redis-password
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0

# PostgreSQL (compose profile: full)
POSTGRES_USER=litellm
POSTGRES_PASSWORD=change-me-postgres-password
POSTGRES_DB=litellm
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# Upstream OpenCode Go accounts (placeholders only)
OPENCODE_GO_BASE_URL=https://example.invalid/v1
OPENCODE_GO_KEY_A=
OPENCODE_GO_KEY_B=

# Upstream Volcengine Coding Plan (placeholders only)
VOLC_CODING_BASE_URL=https://example.invalid/v1
VOLC_CODING_KEY_C=
